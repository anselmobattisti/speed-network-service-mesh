# Instalation

File to create the environment where the SPEED will be executed

## Alpine

To install the Alpine inside each cluster to execute the use-cases run the installation inside the script/alpine folder.

## Environment

To install the environment where the speed will be executed run the installation inside the script/environment folder.

## Basic NSM

To install the NSM components inside each cluster to execute the use-cases run the installation inside the script/basic folder.